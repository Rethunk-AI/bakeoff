---
ticket: 22
type: comment
comment_id: 4574170103
author: gissf1
posted: 2026-05-29T11:00:42Z
topic: uuids
url: https://github.com/Rethunk-AI/bakeoff/issues/22#issuecomment-4574170103
---

RE: schema_hops: I dislike that name, and I like the name "table_migrations" better, but I think the fields should actually be merged with schema_versions.  And we probably don't need schema_version_history and should instead add fields to schema_tables for "schema_version_id" (FK to schema_versions table) and "schema_applied" (for what was `schema_version_history.applied_at`).  I feel like this whole thing is becoming a mess though, I want to discuss this more before any additional steps are taken on it and surely before approval is given on these tables.

I'm not fully settled on the UUID namespace issue yet, but I think UUID changes to any table can simply be migrated to a new table using a new UUID namespace.

Per your suggestion, let's start a new thread for discussing the table/schema migration tables and the remainder of that design.  On the new thread, let's again show the table designs before the above comments, what they changed to after the above comments, and we can proceed from there with the discussion.
